I make my comments inline in `Risks & Ambiguities` section. 

Also See my comments and questions:

1. What is a `Protocol`? It is not clear to me.
2. I do not like the verb `INJECT`, let's use `INSERT`.
3. For simulator, let's add explict validation that all passengers are served eventually (by the max ticks).
4. For the CLI, let's remove the compare command. The comparisons should be plotted in ONE chart for all strategies and workloads.
  a. The CLI must support running multiple workloads.
5. Let's benchmark the system a 500 person building, with 20 floors and 6 elevators. Estimate the number of ticks that make sense for the scenarios. 

Discuss with me and address them.
